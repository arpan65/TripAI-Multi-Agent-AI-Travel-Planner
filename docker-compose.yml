services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./travel_runs.db:/app/travel_runs.db
      - ms-playwright:/ms-playwright
    restart: unless-stopped

volumes:
  ms-playwright:
